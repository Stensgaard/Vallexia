meta {
  name: updating daily calories to 0
  type: http
  seq: 1
}

put {
  url: {{baseUrl}}/api/{{apiVersion}}/users/nutritional-goals
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "dailyCalories": 0.0,
    "dailyProtein": 150.0,
    "dailyCarbs": 200.0,
    "dailyFats": 65.0
  }
}

tests {
  test("Status is 400 Bad Request", function() {
    expect(res.status).to.equal(400);
  });
  
  test("Daily calories validation error for zero", function() {
    expect(res.body.details).to.have.property("dailyCalories");
    expect(res.body.details.dailyCalories).to.satisfy(msg => "Daily calories must be greater than");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
