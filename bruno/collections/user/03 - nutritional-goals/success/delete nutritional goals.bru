meta {
  name: delete nutritional goals
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/{{apiVersion}}/users/nutritional-goals
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status is 204 No Content", function() {
    expect(res.status).to.equal(204);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
