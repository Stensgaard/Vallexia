meta {
  name: missing daily calories
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/api/{{apiVersion}}/users/nutritional-goals
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "dailyProtein": 150.0,
    "dailyCarbs": 200.0,
    "dailyFats": 65.0
  }
}

tests {
  test("Status is 400 Bad Request", function() {
    expect(res.status).to.equal(400);
  });
  
  test("Daily calories validation error in details", function() {
    expect(res.body.details).to.have.property("dailyCalories");
    expect(res.body.details.dailyCalories).to.include("required");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
