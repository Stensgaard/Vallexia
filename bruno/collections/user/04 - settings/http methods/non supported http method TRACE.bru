meta {
  name: non supported http method TRACE
  type: http
  seq: 4
}

trace {
  url: {{baseUrl}}/api/{{apiVersion}}/users/settings
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status is 405 Method Not Allowed", function() {
    expect(res.status).to.equal(405);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
