meta {
  name: updating currency to invalid value
  type: http
  seq: 7
}

put {
  url: {{baseUrl}}/api/{{apiVersion}}/users/settings
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "language": "en",
    "country": "DK",
    "dateFormat": "DD_MM_YYYY_DOT",
    "timezone": "Europe/Copenhagen",
    "firstDayOfWeek": "SUNDAY",
    "measurementSystem": "METRIC",
    "currency": "abcd"
  }
}

tests {
  test("Status is 400 Bad Request", function() {
    expect(res.status).to.equal(400);
  });
  
  test("Currency validation error with invalid value", function() {
    expect(res.body.details).to.have.property("currency");
    expect(res.body.details.currency).to.satisfy(msg => "Currency must be one of the supported locales");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
