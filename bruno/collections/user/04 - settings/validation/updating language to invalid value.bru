meta {
  name: updating language to invalid value
  type: http
  seq: 1
}

put {
  url: {{baseUrl}}/api/{{apiVersion}}/users/settings
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "language": "invalid",
    "country": "DK",
    "dateFormat": "DD_MM_YYYY_DOT",
    "timezone": "Europe/Copenhagen",
    "firstDayOfWeek": "SUNDAY",
    "measurementSystem": "METRIC",
    "currency": "DKK"
  }
}

tests {
  test("Status is 400 Bad Request", function() {
    expect(res.status).to.equal(400);
  });
  
  test("Language validation error with invalid value", function() {
    expect(res.body.details).to.have.property("language");
    expect(res.body.details.language).to.satisfy(msg => "Locale must be one of the supported locales");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
