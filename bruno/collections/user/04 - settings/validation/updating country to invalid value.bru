meta {
  name: updating country to invalid value
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/api/{{apiVersion}}/users/settings
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "language": "en",
    "country": "invalid",
    "dateFormat": "DD_MM_YYYY_DOT",
    "timezone": "Europe/Copenhagen",
    "firstDayOfWeek": "SUNDAY",
    "measurementSystem": "METRIC",
    "currency": "DKK"
  }
}

tests {
  test("Status is 400 Bad Request", function() {
    expect(res.status).to.equal(400);
  });
  
  test("Country validation error with invalid value", function() {
    expect(res.body.details).to.have.property("country");
    expect(res.body.details.country).to.satisfy(msg => "country must be one of the supported locales");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
