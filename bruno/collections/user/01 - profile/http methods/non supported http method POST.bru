meta {
  name: non supported http method POST
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/users/profile
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
