meta {
  name: missing username
  type: http
  seq: 1
}

put {
  url: {{baseUrl}}/api/{{apiVersion}}/users/profile
  body: json
  auth: inherit
}

body:json {
  {
    "householdSize": 2,
    "mealTypes": ["BREAKFAST", "LUNCH", "DINNER"]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
