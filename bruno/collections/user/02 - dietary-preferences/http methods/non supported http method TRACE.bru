meta {
  name: non supported http method TRACE
  type: http
  seq: 3
}

trace {
  url: {{baseUrl}}/api/{{apiVersion}}/users/dietary-preferences
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status is 405 Method Not Allowed", function() {
    expect(res.status).to.equal(405);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
