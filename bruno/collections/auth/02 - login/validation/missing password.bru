meta {
  name: missing password
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "usernameOrEmail": "{{registeredUsername}}",
    "password": ""
  }
}

tests {
  test("Status is 400 Bad Request", function() {
    expect(res.status).to.equal(400);
  });
  
  test("Password validation error in details", function() {
    expect(res.body.details).to.have.property("password");
    expect(res.body.details.password).to.include("required");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
