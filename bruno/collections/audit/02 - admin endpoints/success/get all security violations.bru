meta {
  name: get all security violations
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/audit-logs/security-violations?page=0&size=20
  body: none
  auth: bearer
}

params:query {
  page: 0
  size: 20
}

auth:bearer {
  token: {{adminAccessToken}}
}

tests {
  test("Status is 200 OK (requires ADMIN role)", function() {
    expect(res.status).to.equal(200);
  });
  
  test("Response contains security violations", function() {
    expect(res.body).to.have.property("content");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
