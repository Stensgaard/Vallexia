meta {
  name: invalid date format
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/audit-logs/date-range?start=2024-01-01&end=2024-01-31
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

tests {
  test("Status is 400 Bad Request", function() {
    expect(res.status).to.equal(400);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
