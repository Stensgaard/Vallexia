meta {
  name: missing access token
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/audit-logs/me?page=0&size=20
  body: none
  auth: inherit
}

params:query {
  page: 0
  size: 20
}

tests {
  test("Status is 401 Unauthorized", function() {
    expect(res.status).to.equal(401);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
