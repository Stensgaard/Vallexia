meta {
  name: unauthorized - missing token
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/audit-logs/failed-logins?username={{registeredUsername}}
  body: none
  auth: none
}

tests {
  test("Status is 401 Unauthorized", function() {
    expect(res.status).to.equal(401);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
