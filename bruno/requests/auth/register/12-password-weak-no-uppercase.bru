meta {
  name: Register - Password Weak No Uppercase
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/api/v1/auth/register
  body: json
  auth: none
}

body:json {
  {
    "username": "testuser",
    "email": "test@example.com",
    "password": "testpass123!",
    "confirmPassword": "testpass123!"
  }
}

tests {
  test("Status is 400 Bad Request", function() {
    expect(res.status).to.equal(400);
  });

  test("Password validation error for missing uppercase", function() {
    expect(res.body.details).to.have.property("password");
    expect(res.body.details.password).to.satisfy(msg => 
      msg.includes("uppercase") || msg.includes("character") || msg.includes("Password must")
    );
  });
}
