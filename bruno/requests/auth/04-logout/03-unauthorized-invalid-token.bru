meta {
  name: Logout - Unauthorized Invalid Token
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/auth/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: invalid.token.here
}

tests {
  test("Status is 403 Forbidden", function() {
    expect(res.status).to.equal(403);
  });
}
