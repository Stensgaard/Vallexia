meta {
  name: Logout - Unauthorized Missing Token
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/auth/logout
  body: none
  auth: none
}

tests {
  test("Status is 403 Forbidden", function() {
    expect(res.status).to.equal(403);
  });
}
