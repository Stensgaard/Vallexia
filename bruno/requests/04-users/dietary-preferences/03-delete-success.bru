meta {
  name: Delete Dietary Preferences - Success
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/v1/users/dietary-preferences
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status is 204 No Content", function() {
    expect(res.status).to.equal(204);
  });
}
