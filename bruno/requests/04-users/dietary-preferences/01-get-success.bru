meta {
  name: Get Dietary Preferences - Success
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/users/dietary-preferences
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status is 200 OK", function() {
    expect(res.status).to.equal(200);
  });

  test("Response contains userId", function() {
    expect(res.body).to.have.property("userId");
  });
}
