meta {
  name: Delete Nutritional Goals - Success
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/v1/users/nutritional-goals
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status is 204 No Content", function() {
    expect(res.status).to.equal(204);
  });
}
