meta {
  name: Update Nutritional Goals - Protein Percentage Too High
  type: http
  seq: 11
}

put {
  url: {{baseUrl}}/api/v1/users/nutritional-goals
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "dailyCalories": 2000.0,
    "dailyProtein": 150.0,
    "dailyCarbs": 200.0,
    "dailyFats": 65.0,
    "proteinPercentage": 101.0
  }
}

tests {
  test("Status is 400 Bad Request", function() {
    expect(res.status).to.equal(400);
  });

  test("Protein percentage validation error for too high", function() {
    expect(res.body.details).to.have.property("proteinPercentage");
    expect(res.body.details.proteinPercentage).to.satisfy(msg => 
      msg.includes("100") || msg.includes("exceed") || msg.includes("not exceed")
    );
  });
}
